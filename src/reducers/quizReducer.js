import { TEST_FETCHED } from '../actions/constants';

const intitialState = {

}


export default function QuizReducer (state = initialState, action) {
  switch(action.type) {
    default:
      return state



  }
}